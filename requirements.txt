# MacroRisk AI — ML backend dependencies
# Phase 1 (data discovery & canonical dataset) only needs the core stack below.
# Additional modelling libraries will be added in later phases.

pandas>=2.0
numpy>=1.24
scipy>=1.10
PyYAML>=6.0

# Optional: required only if source datasets are provided as Excel workbooks.
# The loader auto-detects format; install if you feed .xlsx/.xls inputs.
openpyxl>=3.1
